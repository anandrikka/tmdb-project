import axios from 'axios';

import * as ActionConstants from './action.constants';
import {
    RESOURCE_TODAY_SERIALS,
    RESOURCE_TV_TOP_RATED,
    RESOURCE_TV_ON_AIR,
    RESOURCE_TV_POPULAR
} from '../Utilities/Urls';

export const tvList = (tv, page) => ({

})